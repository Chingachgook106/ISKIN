# CONTEXT COMPRESSION & STRUCTURED BRIEF: ISKIN PROJECT
## Session: RU-001-NB-001 (Network Baseline Execution)

### 1. Roles & Communication Rules
- **User:** Манни (Manni / Мануэль Гарсия О’Келли-Дэвис) — Customer, Co-lead Developer.
- **AI Persona:** Асси (Assi) — Lead Engineer of the ISKIN project.
- **Tone & Style:** Informal ("ты", like old acquaintances), methodical, strict about facts. No unverified info. Direct correction of technical errors.
- **Core Engineering Rule:** No changes "just in case". Every config change must follow: Почему / Что / Зачем / Проверка.

### 2. Project Architecture (ISKIN ADR-001)
- **EMS-001 ("Малыш"):** Control / Orchestration Node (Beelink5800H, Win 11 Pro 25H2 Build 26200). Must remain autonomous and lightweight. NOT a Compute Node.
- **Compute Node:** Future separate hardware for heavy GPU/LLM tasks.
- **CR-001 (SMB Exchange):** CLOSED. Share `\\192.168.0.102\iskin-exchange` mapped to `C:\iskin-exchange`.

### 3. Completed Task: TZ RU-001-NB-001 (Network Baseline)
**Goal:** Capture a read-only snapshot of EMS-001's network state before implementing the RU-001 privacy/security layer.
**Status:** ✅ COMPLETED. Final report `RU-001-Network-Baseline-001.md` generated and approved by the Chief Architect.
**Constraint Adherence:** EMS-001 configuration was strictly NOT changed during diagnostics.

### 4. Key Technical Findings (The Baseline State)
- **Interfaces:** `Ethernet 4` (Realtek) is UP (`192.168.0.102/24`, GW `192.168.0.1`). Wi-Fi/BT are DOWN (APIPA).
- **DNS Configuration:** Hardcoded to public resolvers `8.8.8.8` and `1.1.1.1` on Ethernet 4. (High DNS exposure / leak vector).
- **IPv6 State:** Stack enabled in OS, but NO global IPv6 addresses, NO IPv6 default route (`::/0` missing). Pure IPv4 transit.
- **Routing:** Clean, standard IPv4 table. Single default route via `192.168.0.1`. No hidden static routes.
- **Public IP:** IPv4 present and active (REDACTED in logs). IPv6 absent.
- **Connectivity:** GitHub HTTPS (TCP 443) works. `whoami.akamai.net` resolves to real public IP (DNS leak confirmed).

### 5. Incidents & Workarounds Handled
- **SMB UI Glitch (WMP Prompt):** Windows 11 Explorer "Network" node falsely demands Windows Media Player installation to view SMB shares. 
  - *Resolution:* Bypassed by using direct UNC paths (`\\192.168.0.X\share`) or local paths (`C:\iskin-exchange\outgoing`). WMP was NOT installed to preserve baseline integrity.
- **OpenDNS Anomaly:** Direct `nslookup` to `resolver1.opendns.com` yielded no output (silent drop/timeout), while Google/Cloudflare worked fine. Logged as an ISP DPI/routing anomaly.

### 6. Pending & Next Steps (For Current Session)
1. **NEW TZ (Priority 1):** Awaiting new Technical Assignment from the Chief Architect (based on the baseline analysis). This will dictate the architectural selection and implementation of the RU-001 network layer (System VPN / Proxy / Overlay).
2. **iskin-docs Repository (Pending):** Decision on where to host the Git repo (Local / Main PC / GitHub) is still technically pending, but may be superseded or influenced by the new RU-001 TZ.

### 7. Rules to Carry Over
- Security ≠ Privacy ≠ Anonymization.
- Baseline is the absolute source of truth for network diagnostics.
- Do not install heavy server components/Docker on EMS-001 without explicit ADR.